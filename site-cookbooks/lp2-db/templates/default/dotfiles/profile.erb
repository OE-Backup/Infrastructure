# ~/.profile: executed by the command interpreter for login shells.
# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# the default umask is set in /etc/profile; for setting the umask
# for ssh logins, install and configure the libpam-umask package.
#umask 022

# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
        . "$HOME/.bashrc"
    fi
fi

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi

# set PATH so it includes PostgreSQL's private bin if it exists
if [ -d "/usr/lib/postgresql/9.2/bin" ] ; then
    PATH="/usr/lib/postgresql/9.2/bin:$PATH"
fi

#export export PGHOST=`hostname -f`
export PGUSER=postgres
export PGPORT=5432
export PGOPTIONS=
export PGHBA=/etc/postgresql/9.2/main
export PGDATA=/var/lib/postgresql/9.2/main
export PGEXEC=/var/lib/postgresql/ejecuciones
export PGLOG=/mnt/postgresql/log
export PGARCH=/mnt/postgresql/archived_wal
export PGBKP=/mnt/backups/postgresql/daily
export PGBACKUPS=/mnt/backups/postgresql
export PGDUMPS=/mnt/postgresql/pg_dump
